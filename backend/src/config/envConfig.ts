import * as dotenv from 'dotenv'

dotenv.config()

export default {
    // JWT_SECRET: process.env.JWT_SECRET,
    // JWT_expires: process.env.JWT_EXPIRATION,
    // RefreshToken_expires_days: 30,
    // APP_PORT: process.env.APP_PORT
}