import styled from "styled-components";

export const PopoverMenuContainer = styled.div`

    button {
        svg {
            font-size: 2rem;
        }
    }

    .popoverMenuContent{
        position: absolute;
        right: 0;

        padding: 0.5rem 1rem;
    }
`