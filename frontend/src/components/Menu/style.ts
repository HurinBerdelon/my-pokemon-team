import styled from "styled-components";

export const MenuContainer = styled.nav`
    margin-top: 1rem;    
    display: flex;
    flex-direction: column;
    gap: 0.25rem;

    a {
        font-size: 1.25rem;
    }
`